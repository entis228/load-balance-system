export const TaskList = (props)=>{

    return (
      <div>
          <p>{"Список рекомендованих завдань"}</p>

          {/*<ul>*/}
          {/*    {props.tasks.map(task=>(*/}
          {/*        <li key={task}>{task}</li>*/}
          {/*    ))}*/}
          {/*</ul>*/}
      </div>
    );
}